# UQC-217 — G07 exact-geometry reproduction and scoped repair

Status: Done (2026-09-16; verified QtQuick ownership handoff, G07 unrepaired).

## Assignment

Greeter baseline `b7277a63625e07e74f491839ce5b5f6dd4dfdad6`, provider
`0e0f92efe1517bc07c577299f07b51117f25f05a`, configuration
`fe69a59e6b73167fd5349223a4d265d75386c139` are clean and confirmed on canonical
main with `git ls-remote` on 2026-09-16. Ready prerequisites satisfied before work.

## Scope and acceptance

Reproduce the September 16 geometry in production QML: DPR 1, 1276×1571,
mapped cursor (771.616,789.111,0.78,17.94); DPR 1.25, 1021×1257,
(543.136,633.026,0.78,17.94). Assert dimensions, DPR, panel scale 0.78,
field 420×57 and cursor mapping. Retain full-window on/off and difference images.
Compare both selected styles in software and private Sway/OpenGL with staged
theme. Vary minimal-field position, scale, font and padding independently.
Preserve failure before any proven greeter-local repair. If shared Qt/provider
behavior is responsible, deliver reproduction and an ownership handoff instead.

Cover empty, populated, cleared, focus loss/re-entry and reveal/remasking;
retain normal/compact cases. A repair requires full suite, import policy,
formatting, QML lint/types, static analysis, licensing, build and relocated
launch verification, publication before pinning, and a fresh immutable repair
kit. No unchanged diagnostic kit. G07 closes only after four human-operated
comparisons. Preserve G01–G06, closed Batch 7, UQC-216 Done, UQC-201 In Progress
and initiative Accepted. No public API, schema or authentication changes.

## Result and ownership handoff — 2026-09-16

The exact geometry reproduces G07. The same failure occurs in a plain QtQuick
`TextInput` with no Controls import and only the root content item as ancestor.
This establishes a shared QtQuick rendering boundary; greeter composition and
HoloNight's selected TextField implementation are not required. The precise Qt
rasterization mechanism is not yet diagnosed. Qt version is 6.11.2; graphics runs
use OpenGL/RHI, Mesa 26.2.2 llvmpipe (LLVM 22.1.8), private headless Sway and the
staged HoloNight platform theme. Software runs use the isolated offscreen backend.
No production repair or provider assignment is justified by this handoff.

### Recorded results

Both software and graphics, both selected styles, satisfy the exact window,
DPR, field-size, panel-scale and cursor-position assertions. Counts below are
changed pixels near the mapped cursor in full-window caret-on/off comparisons.

| Phase | DPR 1, both styles/backends | DPR 1.25, both styles/backends |
|---|---:|---:|
| Production empty / cleared / refocused-remasked | 0 / 0 / 0 | 23 / 23 / 23 |
| Production populated, disposable character | 18 | 46 software, 23 graphics |
| Unfocused / reveal held without field focus | 0 / 0 | 0 / 0 |
| Minimal selected-style equivalent | 0 | 23 |
| Plain QtQuick equivalent | 0 | 23 |

At DPR 1, changing only the minimal field font retains zero pixels in both
styles. Default left padding restores 18 pixels in HoloNight but still gives
zero in Fusion. Rounding only the field position restores 18 pixels in both;
removing only its scale restores 23. These variants expose position/scale
sensitivity; they are not robust product repairs and have not been applied to
production. The equivalent plain TextInput has the same native cursor rectangle,
font, alignment, padding, mapped position and scale as production.

The regression remains deliberately failing at DPR 1, with its visibility
assertions intact. It is named `DISABLED_PasswordCaretRecordedGeometry` so the
known external issue does not disable ordinary CI. Explicitly enabling it must
fail on the affected Qt baseline; a green standard suite is not G07 acceptance.
Enable it normally only after the owning-layer repair passes both DPRs/styles.
The normal/compact `PasswordCaretPixels` coverage stays enabled.

### Reproduction commands

From the greeter root, using the existing pinned dependency build:

```sh
cmake --build build -j4
QT_QUICK_CONTROLS_STYLE=Holonight QT_SCALE_FACTOR=1 \
  GREETER_CARET_ARTIFACTS=/tmp/uqc217-software-Hol-1 \
  python3 scripts/run-isolated-test.py build/greeter_runtime_tests \
  --gtest_filter=RuntimeControls.DISABLED_PasswordCaretRecordedGeometry \
  --gtest_also_run_disabled_tests
python3 scripts/check-caret-graphics.py build/greeter_runtime_tests \
  build/dependencies/prefix --logs /tmp/uqc217-graphics --exact-geometry
```

Repeat software with Fusion and DPR 1.25. The graphics command runs all four
cases, reports each process exit, then returns failure for the two DPR-1 cases.
Omit `--exact-geometry` for normal/compact graphics coverage. Use fresh artifact
directories: each check saves full-window `-on.png`, `-off.png`, and a binary
`-diff.png`; logs retain geometry, phases and pixel counts, never entered text.
Only the fixture's disposable character is rendered in populated captures.

The harness now uses a declarative floating-window rule in its private compositor
and waits for initial Wayland exposure before resizing. Earlier asynchronous
Sway configure events replaced dimensions (2556×1571, 2045×1257, or 1672×941);
those runs correctly fail geometry assertions and are excluded from reproduction
claims. No desktop pointer or focus interaction is automated.

### Next owner and acceptance

Hand this reproduction to QtQuick text/caret rendering investigation. First
reproduce the plain TextInput and determine how a transformed one-logical-pixel
native caret is rasterized at the recorded positions. Preserve native blink,
masking, palette, navigation and fractional appearance in any proposed repair.
A HoloNight provider change requires separate evidence and assignment. Neither
repository is modified speculatively here, and no upstream issue is submitted
on the user's behalf.

The plan's ownership-handoff branch replaces the repair-kit branch. There is no
repair kit or request to repeat the previous human diagnostic runs. G07 stays
open for a later proven repair and four focused human-operated comparisons;
UQC-216 Done, G01–G06 and closed Batch 7 remain unchanged.

## Verification evidence

Umbrella `.cache/uqc217/` retains the attempts and final evidence:
`software-handoff-*` and `graphics-handoff/` contain the eight exact cases,
`verification.json` summarizes all 12 phases per case, and `SHA256SUMS` covers
298 evidence files, including full-window on/off/difference images. Manifest hash:
`99f5c0d489596e7b18fc0ad5a29c414159ccfcecb99a9cdcd7f55898f61611e9`.
All four DPR-1 runs exit 1 with the preserved visibility failure; all four
DPR-1.25 runs exit 0. This is successful reproduction, not repair acceptance.

Build, formatting, QML lint/types and static analysis of all 15 owned translation
units pass. REUSE reports 85/85 files compliant. Sandboxed fake-socket tests and
REUSE multiprocessing failed due to socket restrictions; unrestricted reruns
pass. Initial Sway startup/configure failures are retained separately and excluded
from the final eight-case results. `git diff --check` passes.

Build/relocated-install executable launches and immutable repair-kit preparation
are not repeated: no production executable, QML, dependency, installation layout
or authentication behavior changes in this ownership-handoff branch. A later
repair must complete those checks before release and human comparisons.

Final standard suite: `ctest --test-dir build --output-on-failure` passes 8/8
(`ctest-final.log`), including both styles/DPRs and import-policy fixtures.
Final normal/compact graphics matrix passes 4/4 (`graphics-normal/`), with actual
window-size assertions. Python harness compilation passes. The four explicit
DPR-1 reproduction failures remain intentional and are not counted as passes.
Canonical publication and the umbrella checkpoint are recorded in the umbrella
ledger after publication; Done here denotes the verified ownership handoff.
